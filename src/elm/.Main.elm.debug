module Main exposing (main)

import App exposing (..)
import TimeTravel.Html as Html
import View exposing (view)


main =
    Html.program
        { init = init
        , update = update
        , view = view
        , subscriptions = subscriptions
        }
